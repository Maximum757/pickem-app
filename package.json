{
  "name": "contrarian-pick-em",
  "version": "1.0.0",
  "description": "Contrarian NFL/college pick 'em web app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "tsc -p tsconfig.engine.json && cp dist-engine-package.json dist-engine/package.json && node dist-engine/pick-em-simulation.js",
    "seed:week0": "tsc -p tsconfig.seed.json && cp dist-engine-package.json dist-seed/package.json && node --env-file=.env.local dist-seed/seed-week0.js",
    "export": "tsc -p tsconfig.export.json && cp dist-engine-package.json dist-export/package.json && node --env-file=.env.local dist-export/export-data.js",
    "seed:weeks1-8": "tsc -p tsconfig.weeks1-8.json && cp dist-engine-package.json dist-weeks1-8/package.json && node --env-file=.env.local dist-weeks1-8/seed-weeks1-8.js",
    "seed:weeks9-18": "tsc -p tsconfig.weeks9-18.json && cp dist-engine-package.json dist-weeks9-18/package.json && node --env-file=.env.local dist-weeks9-18/seed-weeks9-18.js",
    "patch:spreads": "tsc -p tsconfig.patch-spreads.json && cp dist-engine-package.json dist-patch-spreads/package.json && node --env-file=.env.local dist-patch-spreads/patch-spreads.js",
    "refresh:spreads": "tsc -p tsconfig.refresh-spreads.json && cp dist-engine-package.json dist-refresh-spreads/package.json && node --env-file=.env.local dist-refresh-spreads/refresh-spreads.js",
    "lock:passed-games": "tsc -p tsconfig.lock-passed-games.json && cp dist-engine-package.json dist-lock-passed-games/package.json && node --env-file=.env.local dist-lock-passed-games/lock-passed-games.js",
    "backfill:visibility": "tsc -p tsconfig.backfill-visibility.json && cp dist-engine-package.json dist-backfill-visibility/package.json && node --env-file=.env.local dist-backfill-visibility/backfill-visibility.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "firebase": "^10.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "autoprefixer": "^10.4.0",
    "firebase-admin": "^12.0.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}